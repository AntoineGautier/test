name: Test

on:
  pull_request:
    branches:
      - 'master'
  push:
    branches:
      - 'master'

env:
  BUILD_TYPE: Release
  GH_USERNAME: ${{ github.actor }}

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
      - uses: actions/checkout@v3

      - name: Debug
        run: |
          echo ${{ github.event.name }}
          echo ${{ github.event_name }}
          echo ${{ github.ref }}

      - name : machine echo $GITHUB_EVENT_NAME
        run : "echo $GITHUB_EVENT_NAME"
      - name : machine echo $GITHUB_BASE_REF
        run : "echo $GITHUB_BASE_REF"
      - name : machine echo $GITHUB_HEAD_REF
        run : "echo $GITHUB_HEAD_REF"
      - name : machine echo $GITHUB_REF
        run : "echo $GITHUB_REF"
      - name : machine echo 1
        run : "echo ${{ github.event.name == 'push' }}"
      - name : machine echo 2
        run : "echo ${{ github.ref == 'refs/heads/master' }}"

      - name: Push binaries
        if: |
          contains(matrix.os, 'latest') &&
          (github.event.name == 'push') &&
          (github.ref == 'refs/heads/master')
        run: |
          git config --global user.name $GH_USERNAME
          git config --global user.email $GH_USERNAME@users.noreply.github.com
          git config --global pull.rebase false
          git pull
          git diff-index --quiet HEAD || git commit --allow-empty -m "Add ${{ matrix.os }} binaries"
          git push
